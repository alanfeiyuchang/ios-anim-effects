enum MorphEffects {
    static let all: [Effect] = [
        .morphButtonToCard,
        .morphHeroCard,
        .morphNativeZoom,
        .morphFabMenu,
        .morphZoomSheet,
        .morphMiniPlayer,
        .morphFolderOpen,
        .morphSearchExpand,
        .morphIslandExpand,
        .morphGalleryZoom,
        .morphShapeMorph,
        .morphPolygonSides,
        .morphCornerCascade,
        .morphLineToRing,
        .morphCircularReveal,
        .morphBlurReplace,
        .morphBlindsReveal,
        .morphFeatherWipe,
        .morphTileMosaic,
        .morphStaggeredTransition,
        .morphListGrid,
        .morphCubeTransition,
        .morphGridToRing,
        .morphSortHop,
        .morphLiquidGlass,
    ]
}
