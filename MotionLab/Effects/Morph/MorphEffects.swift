enum MorphEffects {
    static let all: [Effect] = [
        .morphButtonToCard,
        .morphHeroCard,
        .morphFabMenu,
        .morphZoomSheet,
        .morphSearchExpand,
        .morphGalleryZoom,
        .morphShapeMorph,
        .morphCircularReveal,
        .morphBlurReplace,
        .morphStaggeredTransition,
        .morphListGrid,
        .morphCubeTransition,
        .morphLiquidGlass,
    ]
}
