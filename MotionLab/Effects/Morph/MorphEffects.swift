enum MorphEffects {
    static let all: [Effect] = [
        .morphButtonToCard,
        .morphHeroCard,
        .morphFabMenu,
        .morphZoomSheet,
        .morphMiniPlayer,
        .morphFolderOpen,
        .morphSearchExpand,
        .morphGalleryZoom,
        .morphShapeMorph,
        .morphCircularReveal,
        .morphBlurReplace,
        .morphStaggeredTransition,
        .morphListGrid,
        .morphCubeTransition,
        .morphLiquidGlass,
    ]
}
