enum MorphEffects {
    static let all: [Effect] = [
        .morphButtonToCard,
        .morphHeroCard,
        .morphNativeZoom,
        .morphFabMenu,
        .morphZoomSheet,
        .morphMiniPlayer,
        .morphFolderOpen,
        .morphSearchExpand,
        .morphGalleryZoom,
        .morphShapeMorph,
        .morphCircularReveal,
        .morphBlurReplace,
        .morphStaggeredTransition,
        .morphListGrid,
        .morphCubeTransition,
        .morphLiquidGlass,
    ]
}
