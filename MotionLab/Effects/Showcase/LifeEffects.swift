import SwiftUI

/// Everyday-life dashboards (money, sleep) in the same dark "Signature Interactions" style,
/// so the category is not only about outdoor trips.
enum ShowcaseLifeEffects {
    static let all: [Effect] = [
        .showcaseFinanceCard,
        .showcaseSleepTimeline,
    ]
}
